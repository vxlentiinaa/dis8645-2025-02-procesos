#include "SensorTemperatura.h"

//ver si puedo solucionar el error de compilación "Serial" no fue declarado
#include <Arduino.h>

void configurarSensorTemperatura() {
  // conectar la patita

  // definir la temperatura minima
  // definir la temperatura maxima

}

void leerTemperatura() {
  // Serial.read(temperaturaMinima);
  // Serial.read(temperaturaMaxima);
}