// se describe color Amarillo
// en base a resultado calibración
// se decontruye o traduce en parametros R,G,B

// Color clasificado por R,G,B - se utilizará los datos sin procesar recibidos previamente 
// en archivo CalibracionSensor.ino para mostrar los valores RGB del color detectado

// se pone maximos y minimos en base a cada parametro del color (R,G,B)
if ( Red < -- && Green > -- && Blue > -- ) 